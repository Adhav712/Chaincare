import 'package:flutter/material.dart';

class DoctorLogin extends StatelessWidget {
  const DoctorLogin({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text("Doctor Login");
  }
}
